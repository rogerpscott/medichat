


<div class="container">
  <div class="row">
    <div class="col-xs-offset-1 col-xs-10 col-sm-offset-3 col-sm-6">
      <div id="background-form">
        <h2>¿Cual fue su diagnostico?</h2>

        <%= simple_form_for(@conversation, html: { novalidate: false }) do |f| %>
        <div class="form-inputs">
          <%= f.input :diagnosis, label: :Diagnostico, required: true, autofocus: true, pattern: :"[a-zA-Z0-9]{3,}" %>
          <%= f.input :summary, label: "Resumen para el paciente", required: true, pattern: :"[a-zA-Z0-9]{3,}" %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Terminar" %>
        </div>
        <% end %>

      </div>
    </div>
  </div>
</div>
